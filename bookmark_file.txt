Copy this whole line into your chrome bookmark administrator under new bookmarks.

Without debugging:
javascript:(function(){var totalAdStats={normalAdArea:0,adnamiAdArea:0,totalContentArea:0,viewportsScrolled:0,adsLoaded:0};function highlightAds(adElements){adElements.forEach(function(ad){var overlay=document.createElement('div');overlay.style.position='absolute';overlay.style.top='0';overlay.style.left='0';overlay.style.width='100%';overlay.style.height='100%';overlay.style.backgroundColor='rgb(180, 0, 0)';overlay.style.color='white';overlay.style.fontSize='12px';overlay.style.fontWeight='bold';overlay.style.display='flex';overlay.style.flexDirection='column';overlay.style.justifyContent='center';overlay.style.alignItems='center';overlay.style.pointerEvents='none';overlay.style.zIndex='9999';if(!ad.dataset.adId){ad.dataset.adId='ad-'+Math.random().toString(36).substr(2,9);}var adId=ad.dataset.adId;var rect=ad.getBoundingClientRect();if(!ad.dataset.width||!ad.dataset.height||!ad.dataset.area){var width=Math.max(0,Math.min(window.innerWidth,rect.right)-Math.max(0,rect.left));var height=Math.max(0,Math.min(window.innerHeight,rect.bottom)-Math.max(0,rect.top));var area=width*height;ad.dataset.width=Math.round(width);ad.dataset.height=Math.round(height);ad.dataset.area=Math.round(area);}var screenArea=window.innerWidth*window.innerHeight;var adPercentage=((ad.dataset.area/screenArea)*100).toFixed(1);overlay.innerHTML='<div>TRACKED AD</div><div>ID: '+adId+'</div><div>Width: '+ad.dataset.width+'px</div><div>Height: '+ad.dataset.height+'px</div><div>Area: '+ad.dataset.area+'px²</div><div>Screen Coverage: '+adPercentage+'%</div>';ad.style.position='relative';ad.appendChild(overlay);});}function calculateAdVisibilityOnScroll(debug){var normalAdSlots=Array.prototype.slice.call(document.querySelectorAll('div[id^="google_ads_iframe"], div[id^="div-gpt-ad"]'));var adnamiAdSlots=Array.prototype.slice.call(document.querySelectorAll('div[data-adnm-fid]'));var normalAdArea=0;var adnamiAdArea=0;normalAdSlots.forEach(function(ad){var rect=ad.getBoundingClientRect();if(rect.top<window.innerHeight&&rect.bottom>0){var visibleHeight=Math.max(0,Math.min(window.innerHeight,rect.bottom)-Math.max(0,rect.top));var visibleWidth=Math.max(0,Math.min(window.innerWidth,rect.right)-Math.max(0,rect.left));normalAdArea+=visibleWidth*visibleHeight;ad.dataset.width=Math.round(visibleWidth);ad.dataset.height=Math.round(visibleHeight);ad.dataset.area=Math.round(visibleWidth*visibleHeight);}});adnamiAdSlots.forEach(function(ad){var rect=ad.getBoundingClientRect();if(rect.top<window.innerHeight&&rect.bottom>0){var visibleHeight=Math.max(0,Math.min(window.innerHeight,rect.bottom)-Math.max(0,rect.top));var visibleWidth=Math.max(0,Math.min(window.innerWidth,rect.right)-Math.max(0,rect.left));adnamiAdArea+=visibleWidth*visibleHeight;ad.dataset.width=Math.round(visibleWidth);ad.dataset.height=Math.round(visibleHeight);ad.dataset.area=Math.round(visibleWidth*visibleHeight);}});if(debug){highlightAds(normalAdSlots.concat(adnamiAdSlots));}totalAdStats.normalAdArea+=normalAdArea;totalAdStats.adnamiAdArea+=adnamiAdArea;totalAdStats.totalContentArea+=window.innerHeight*window.innerWidth;totalAdStats.viewportsScrolled++;}function countAdSlots(){window.googletag=window.googletag||{};googletag.cmd=googletag.cmd||[];googletag.cmd.push(function(){var slots=googletag.pubads().getSlots();totalAdStats.adsLoaded=slots.length;});}function getDeviceType(){var width=window.innerWidth;if(width<768)return"Mobile";if(width<1024)return"Tablet";return"Desktop";}function getBrowserType(){var userAgent=navigator.userAgent;if(userAgent.indexOf("Chrome")>-1&&userAgent.indexOf("Edg")==-1)return"Chrome";if(userAgent.indexOf("Safari")>-1&&userAgent.indexOf("Chrome")==-1)return"Safari";if(userAgent.indexOf("Firefox")>-1)return"Firefox";if(userAgent.indexOf("Edg")>-1)return"Edge";return"Other";}function showResultsOverlay(adRatioPercent,contentRatioPercent,totalAdStats){var overlay=document.createElement('div');overlay.style.position='fixed';overlay.style.top='50%';overlay.style.left='50%';overlay.style.transform='translate(-50%, -50%)';overlay.style.backgroundColor='rgba(0, 0, 0, 0.8)';overlay.style.color='white';overlay.style.zIndex='9999';overlay.style.display='flex';overlay.style.flexDirection='column';overlay.style.justifyContent='center';overlay.style.alignItems='center';overlay.style.padding='20px';overlay.style.boxSizing='border-box';if(window.innerWidth<728){overlay.style.width='100%';overlay.style.height='70%';}else{overlay.style.width='60%';overlay.style.height='70%';}var viewportWidth=window.innerWidth;var viewportHeight=window.innerHeight;var deviceType=getDeviceType();var browserType=getBrowserType();overlay.innerHTML='<h1 style="color: white;">Ad Density Report</h1><p>Total Viewports Scrolled: '+totalAdStats.viewportsScrolled+'</p><p>Total Normal Ads Area: '+Math.round(totalAdStats.normalAdArea)+' px²</p><p>Total Adnami Ads Area: '+Math.round(totalAdStats.adnamiAdArea)+' px²</p><p>Total Ad Area: '+Math.round(totalAdStats.normalAdArea+totalAdStats.adnamiAdArea)+' px²</p><p>Total Content Area (Scrolled): '+Math.round(totalAdStats.totalContentArea)+' px²</p><p>Ad Density Ratio: '+((totalAdStats.normalAdArea+totalAdStats.adnamiAdArea)/totalAdStats.totalContentArea)+'</p><p>% of screen used for ads: '+adRatioPercent+'%</p><p>% of screen used for content: '+contentRatioPercent+'%</p><p>Total Ads Loaded: '+totalAdStats.adsLoaded+'</p><h2 style="color: white; margin-top: 20px;">Device Information</h2><p>Viewport Size: '+viewportWidth+'px x '+viewportHeight+'px</p><p>Device Type: '+deviceType+'</p><p>Browser Type: '+browserType+'</p><button id="closeOverlay" style="margin-top: 20px; padding: 10px 20px; font-size: 16px; cursor: pointer;">Close</button>';document.body.appendChild(overlay);document.getElementById('closeOverlay').addEventListener('click',function(){document.body.removeChild(overlay);});}function scrollAndMeasure(delay,debug){delay=delay||1500;debug=!!debug;var currentScroll=0;function nextStep(){if(currentScroll+window.innerHeight<document.body.scrollHeight){window.scrollTo({top:currentScroll,behavior:"smooth"});setTimeout(function(){calculateAdVisibilityOnScroll(debug);currentScroll+=window.innerHeight;nextStep();},delay);}else{window.scrollTo({top:document.body.scrollHeight,behavior:"smooth"});setTimeout(function(){calculateAdVisibilityOnScroll(debug);countAdSlots();setTimeout(function(){var totalAdArea=Math.round(totalAdStats.normalAdArea+totalAdStats.adnamiAdArea);var totalContentArea=Math.round(totalAdStats.totalContentArea);var finalAdDensityRatio=Math.round((totalAdArea/totalContentArea)*100);var adRatioPercent=finalAdDensityRatio;var contentRatioPercent=100-adRatioPercent;console.log("=== FINAL AD DENSITY REPORT ===");console.log("Total Viewports Scrolled:",totalAdStats.viewportsScrolled);console.log("Total Normal Ads Area:",Math.round(totalAdStats.normalAdArea),"px²");console.log("Total Adnami Ads Area:",Math.round(totalAdStats.adnamiAdArea),"px²");console.log("TOTAL Ad Area:",totalAdArea,"px²");console.log("Total Content Area (Scrolled):",totalContentArea,"px²");console.log("FINAL Ad Density Ratio:",finalAdDensityRatio,"%");console.log("% of screen used for ads:",adRatioPercent+"%");console.log("% of screen used for content:",contentRatioPercent+"%");console.log("Total Ads Loaded:",totalAdStats.adsLoaded);showResultsOverlay(adRatioPercent,contentRatioPercent,totalAdStats);},500);},delay);}}nextStep();}scrollAndMeasure(1500,false);})();

With debugging enalbed:
javascript:(function(){var totalAdStats={normalAdArea:0,adnamiAdArea:0,totalContentArea:0,viewportsScrolled:0,adsLoaded:0};function highlightAds(adElements){adElements.forEach(function(ad){var overlay=document.createElement('div');overlay.style.position='absolute';overlay.style.top='0';overlay.style.left='0';overlay.style.width='100%';overlay.style.height='100%';overlay.style.backgroundColor='rgb(180, 0, 0)';overlay.style.color='white';overlay.style.fontSize='12px';overlay.style.fontWeight='bold';overlay.style.display='flex';overlay.style.flexDirection='column';overlay.style.justifyContent='center';overlay.style.alignItems='center';overlay.style.pointerEvents='none';overlay.style.zIndex='9999';if(!ad.dataset.adId){ad.dataset.adId='ad-'+Math.random().toString(36).substr(2,9);}var adId=ad.dataset.adId;var rect=ad.getBoundingClientRect();if(!ad.dataset.width||!ad.dataset.height||!ad.dataset.area){var width=Math.max(0,Math.min(window.innerWidth,rect.right)-Math.max(0,rect.left));var height=Math.max(0,Math.min(window.innerHeight,rect.bottom)-Math.max(0,rect.top));var area=width*height;ad.dataset.width=Math.round(width);ad.dataset.height=Math.round(height);ad.dataset.area=Math.round(area);}var screenArea=window.innerWidth*window.innerHeight;var adPercentage=((ad.dataset.area/screenArea)*100).toFixed(1);overlay.innerHTML='<div>TRACKED AD</div><div>ID: '+adId+'</div><div>Width: '+ad.dataset.width+'px</div><div>Height: '+ad.dataset.height+'px</div><div>Area: '+ad.dataset.area+'px²</div><div>Screen Coverage: '+adPercentage+'%</div>';ad.style.position='relative';ad.appendChild(overlay);});}function calculateAdVisibilityOnScroll(debug){var normalAdSlots=Array.prototype.slice.call(document.querySelectorAll('div[id^="google_ads_iframe"], div[id^="div-gpt-ad"]'));var adnamiAdSlots=Array.prototype.slice.call(document.querySelectorAll('div[data-adnm-fid]'));var normalAdArea=0;var adnamiAdArea=0;normalAdSlots.forEach(function(ad){var rect=ad.getBoundingClientRect();if(rect.top<window.innerHeight&&rect.bottom>0){var visibleHeight=Math.max(0,Math.min(window.innerHeight,rect.bottom)-Math.max(0,rect.top));var visibleWidth=Math.max(0,Math.min(window.innerWidth,rect.right)-Math.max(0,rect.left));normalAdArea+=visibleWidth*visibleHeight;ad.dataset.width=Math.round(visibleWidth);ad.dataset.height=Math.round(visibleHeight);ad.dataset.area=Math.round(visibleWidth*visibleHeight);}});adnamiAdSlots.forEach(function(ad){var rect=ad.getBoundingClientRect();if(rect.top<window.innerHeight&&rect.bottom>0){var visibleHeight=Math.max(0,Math.min(window.innerHeight,rect.bottom)-Math.max(0,rect.top));var visibleWidth=Math.max(0,Math.min(window.innerWidth,rect.right)-Math.max(0,rect.left));adnamiAdArea+=visibleWidth*visibleHeight;ad.dataset.width=Math.round(visibleWidth);ad.dataset.height=Math.round(visibleHeight);ad.dataset.area=Math.round(visibleWidth*visibleHeight);}});if(debug){highlightAds(normalAdSlots.concat(adnamiAdSlots));}totalAdStats.normalAdArea+=normalAdArea;totalAdStats.adnamiAdArea+=adnamiAdArea;totalAdStats.totalContentArea+=window.innerHeight*window.innerWidth;totalAdStats.viewportsScrolled++;}function countAdSlots(){window.googletag=window.googletag||{};googletag.cmd=googletag.cmd||[];googletag.cmd.push(function(){var slots=googletag.pubads().getSlots();totalAdStats.adsLoaded=slots.length;});}function getDeviceType(){var width=window.innerWidth;if(width<768)return"Mobile";if(width<1024)return"Tablet";return"Desktop";}function getBrowserType(){var userAgent=navigator.userAgent;if(userAgent.indexOf("Chrome")>-1&&userAgent.indexOf("Edg")==-1)return"Chrome";if(userAgent.indexOf("Safari")>-1&&userAgent.indexOf("Chrome")==-1)return"Safari";if(userAgent.indexOf("Firefox")>-1)return"Firefox";if(userAgent.indexOf("Edg")>-1)return"Edge";return"Other";}function showResultsOverlay(adRatioPercent,contentRatioPercent,totalAdStats){var overlay=document.createElement('div');overlay.style.position='fixed';overlay.style.top='50%';overlay.style.left='50%';overlay.style.transform='translate(-50%, -50%)';overlay.style.backgroundColor='rgba(0, 0, 0, 0.8)';overlay.style.color='white';overlay.style.zIndex='9999';overlay.style.display='flex';overlay.style.flexDirection='column';overlay.style.justifyContent='center';overlay.style.alignItems='center';overlay.style.padding='20px';overlay.style.boxSizing='border-box';if(window.innerWidth<728){overlay.style.width='100%';overlay.style.height='70%';}else{overlay.style.width='60%';overlay.style.height='70%';}var viewportWidth=window.innerWidth;var viewportHeight=window.innerHeight;var deviceType=getDeviceType();var browserType=getBrowserType();overlay.innerHTML='<h1 style="color: white;">Ad Density Report</h1><p>Total Viewports Scrolled: '+totalAdStats.viewportsScrolled+'</p><p>Total Normal Ads Area: '+Math.round(totalAdStats.normalAdArea)+' px²</p><p>Total Adnami Ads Area: '+Math.round(totalAdStats.adnamiAdArea)+' px²</p><p>Total Ad Area: '+Math.round(totalAdStats.normalAdArea+totalAdStats.adnamiAdArea)+' px²</p><p>Total Content Area (Scrolled): '+Math.round(totalAdStats.totalContentArea)+' px²</p><p>Ad Density Ratio: '+((totalAdStats.normalAdArea+totalAdStats.adnamiAdArea)/totalAdStats.totalContentArea)+'</p><p>% of screen used for ads: '+adRatioPercent+'%</p><p>% of screen used for content: '+contentRatioPercent+'%</p><p>Total Ads Loaded: '+totalAdStats.adsLoaded+'</p><h2 style="color: white; margin-top: 20px;">Device Information</h2><p>Viewport Size: '+viewportWidth+'px x '+viewportHeight+'px</p><p>Device Type: '+deviceType+'</p><p>Browser Type: '+browserType+'</p><button id="closeOverlay" style="margin-top: 20px; padding: 10px 20px; font-size: 16px; cursor: pointer;">Close</button>';document.body.appendChild(overlay);document.getElementById('closeOverlay').addEventListener('click',function(){document.body.removeChild(overlay);});}function scrollAndMeasure(delay,debug){delay=delay||1500;debug=!!debug;var currentScroll=0;function nextStep(){if(currentScroll+window.innerHeight<document.body.scrollHeight){window.scrollTo({top:currentScroll,behavior:"smooth"});setTimeout(function(){calculateAdVisibilityOnScroll(debug);currentScroll+=window.innerHeight;nextStep();},delay);}else{window.scrollTo({top:document.body.scrollHeight,behavior:"smooth"});setTimeout(function(){calculateAdVisibilityOnScroll(debug);countAdSlots();setTimeout(function(){var totalAdArea=Math.round(totalAdStats.normalAdArea+totalAdStats.adnamiAdArea);var totalContentArea=Math.round(totalAdStats.totalContentArea);var finalAdDensityRatio=Math.round((totalAdArea/totalContentArea)*100);var adRatioPercent=finalAdDensityRatio;var contentRatioPercent=100-adRatioPercent;console.log("=== FINAL AD DENSITY REPORT ===");console.log("Total Viewports Scrolled:",totalAdStats.viewportsScrolled);console.log("Total Normal Ads Area:",Math.round(totalAdStats.normalAdArea),"px²");console.log("Total Adnami Ads Area:",Math.round(totalAdStats.adnamiAdArea),"px²");console.log("TOTAL Ad Area:",totalAdArea,"px²");console.log("Total Content Area (Scrolled):",totalContentArea,"px²");console.log("FINAL Ad Density Ratio:",finalAdDensityRatio,"%");console.log("% of screen used for ads:",adRatioPercent+"%");console.log("% of screen used for content:",contentRatioPercent+"%");console.log("Total Ads Loaded:",totalAdStats.adsLoaded);showResultsOverlay(adRatioPercent,contentRatioPercent,totalAdStats);},500);},delay);}}nextStep();}scrollAndMeasure(1500,true);})();